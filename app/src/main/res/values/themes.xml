<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WoCare" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/main</item>
        <item name="colorPrimaryVariant">@color/main</item>
        <item name="colorOnPrimary">@color/main</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Customize your theme here. -->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <style name="btnOk" parent="ThemeOverlay.AppCompat">
        <item name="colorAccent">@color/main</item>
    </style>

    <style name="bottomDialogStyle" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="android:colorBackground">@android:color/transparent</item>
    </style>

    <style name="switchStyle" parent="Theme.WoCare">
        <item name="colorControlActivated">@color/main</item>
        <item name="colorControlNormal">@color/grey_400</item>
        <item name="colorSwitchThumbNormal">@color/grey_400</item>
        <item name="android:colorForeground">@color/grey_400</item>
        <item name="colorAccent">@color/main</item>
    </style>
    
    <style name="ShapeAppearanceOverlay.App.CornerSize20Percent" parent="">
        <item name="cornerSize">20%</item>
    </style>

</resources>